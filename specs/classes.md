# echarts TS interfaces to Language-specific classes

Goal:
- map echarts TypeScript interfaces to each language's class system, providing type checking and validation of inputs
- add methods to convert those classes to JSON compatible with ECharts options
- add methods to render those options as visualizations

## echarts interfaces

echarts source code is available on GitHub: https://github.com/apache/echarts
