% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_density.R
\name{DensityConfig}
\alias{DensityConfig}
\title{Density Chart Configuration}
\usage{
DensityConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  x = NULL,
  group = NULL,
  n = 512L,
  bw = "nrd0",
  na_rm = TRUE,
  palette = NULL,
  xlab = NULL,
  ylab = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{x}{Optional Character: Column to estimate the density of.}

\item{group}{Optional Character: Column to split the estimate by.}

\item{n}{Integer \verb{[2, Inf)}: Points at which the density is estimated.}

\item{bw}{Character: Bandwidth selector, passed to \code{\link[stats:density]{stats::density()}}.}

\item{na_rm}{Logical: If TRUE, drop \code{NA} values before estimating.}

\item{palette}{Optional Character: Series colors, overriding the theme
palette for this chart. \code{NULL} uses the theme's.}

\item{xlab, ylab}{Optional Character: Axis labels. \code{NULL} derives them from
the data.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Plot margins in pixels.}
}
\value{
\code{DensityConfig} object.
}
\description{
A serializable description of a kernel density chart. Build one with
\code{\link[=setup_DensityConfig]{setup_DensityConfig()}} rather than calling this constructor directly.
}
\details{
\code{x} names the column to estimate the density of. \code{group} optionally names a
column to split it by, drawing one curve per level.
}
\examples{
setup_DensityConfig(x = "mpg")@type
}
\author{
EDG
}
