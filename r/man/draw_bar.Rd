% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{draw_bar}
\alias{draw_bar}
\title{Draw a Bar Chart}
\usage{
draw_bar(
  x,
  y,
  palette = NULL,
  stack = FALSE,
  horizontal = FALSE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  theme = NULL,
  margins = DEFAULT_MARGINS,
  width = NULL,
  height = NULL,
  element_id = NULL,
  filename = NULL,
  bar_width = NULL,
  legend_position = "top",
  legend_placement = "outside"
)
}
\arguments{
\item{x}{Character: Category labels.}

\item{y}{Numeric or named list: Bar heights.}

\item{palette}{Optional Character: Bar color or colors. For multiple series,
colors are applied per series and recycled as needed. For a single series,
a single color styles the whole series; multiple colors are recycled
across individual bars. \code{palette} takes precedence over the theme palette.}

\item{stack}{Logical: Whether to stack bars.}

\item{horizontal}{Logical: Whether to draw horizontal bars.}

\item{xlab}{Optional Character: X-axis title (bottom axis).}

\item{ylab}{Optional Character: Y-axis title (left axis).}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override. The palette inside the theme can be
overridden per-chart with the \code{palette} argument.}

\item{margins}{Optional Named numeric vector or named list: Plot margins in
pixels (or percentage strings) for any of \code{"top"}, \code{"right"},
\code{"bottom"}, \code{"left"}. Unspecified sides keep echarts' default auto-sizing.
See \code{\link[=draw_line]{draw_line()}} for details.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{element_id}{Optional Character: Explicit DOM element ID for the widget
container. \code{NULL} lets htmlwidgets generate one.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}

\item{bar_width}{Optional Numeric \verb{(0, Inf)}: Bar thickness in pixels.
NULL lets ECharts size bars from the category spacing.}

\item{legend_position}{Character \{"top", "bottom", "left", "right",
"top-left", "top-right", "bottom-left", "bottom-right"\}: Legend anchor.
Top/bottom anchors use horizontal rows; left/right anchors use a vertical
column. Corner anchors align within the top or bottom row.}

\item{legend_placement}{Character \{"outside", "inside"\}: Relation to the
plotting area. Outside placement reserves space for the complete legend;
inside placement overlays the data. Neither setting adds a missing legend.}
}
\value{
htmlwidget: Widget object.
}
\description{
Quick bar chart from x/y data.
}
\examples{
draw_bar(x = c("A", "B", "C"), y = c(3, 7, 2), title = "Counts")
}
