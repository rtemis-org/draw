% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_learning.R
\name{plot_learning}
\alias{plot_learning}
\title{Plot a Model's Learning Curve}
\usage{
plot_learning(x, ...)
}
\arguments{
\item{x}{\code{rtemis::Supervised}: Fitted regression or classification result.}

\item{...}{Additional arguments to \code{\link[=draw_learning_curve]{draw_learning_curve()}}.}
}
\value{
htmlwidget: ECharts learning curve.
}
\description{
Draw training and validation losses recorded by a rtemis \code{Supervised}
object. This draw-owned S7 generic is separate from \code{rtemis::plot_learning()};
use \code{rtemis.draw::plot_learning()} when both packages are attached.
}
\details{
The method uses \code{rtemis::get_learning_curve()} to extract algorithm-specific
progress, losses, and the selected step, then \code{\link[=draw_learning_curve]{draw_learning_curve()}} to
render them. Forest curves are averaged at each step over available trees.
Models without recorded learning losses produce an informative error.
The default title names the algorithm; pass \code{title = NULL} to omit it or
\code{title = "..."} to supply another title.
}
\examples{
\dontshow{if (requireNamespace("rtemis", quietly = TRUE)) withAutoprint(\{ # examplesIf}
model <- rtemis::train(
  mtcars[, c("wt", "mpg")],
  hyperparameters = rtemis::setup_LINAD(max_leaves = 3L),
  verbosity = 0L
)
rtemis.draw::plot_learning(model)
\dontshow{\}) # examplesIf}
}
