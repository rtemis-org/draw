% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_network.R
\name{setup_NetworkConfig}
\alias{setup_NetworkConfig}
\title{Set up a Network Graph Configuration}
\usage{
setup_NetworkConfig(
  directed = FALSE,
  threshold = NULL,
  self_loops = FALSE,
  layout = "force",
  cluster_resolution = 1,
  scale_by_degree = TRUE,
  color_by_group = FALSE,
  blend_edges = FALSE,
  show_labels = TRUE,
  node_size = 10,
  edge_scale = 3,
  node_opacity = 0.95,
  edge_opacity = 0.4,
  palette = NULL,
  node_color = rtemis_colors[["teal"]],
  positive_color = rtemis_colors[["teal"]],
  negative_color = "#ff9e1f",
  title = NULL,
  dat_path = NULL,
  origin = NULL,
  writer = NULL
)
}
\arguments{
\item{directed}{Logical: Treat the matrix as directed.}

\item{threshold}{Optional Numeric: Drop edges weaker than this.}

\item{self_loops}{Logical: Keep self-edges.}

\item{layout}{Character: Layout algorithm.}

\item{cluster_resolution}{Numeric \verb{[0, Inf)}: Community-detection
resolution. Higher values find more, smaller communities.}

\item{scale_by_degree}{Logical: Size nodes by their degree.}

\item{color_by_group}{Logical: Color nodes by detected community.}

\item{blend_edges}{Logical: Blend overlapping edges.}

\item{show_labels}{Logical: Draw node labels.}

\item{node_size}{Numeric \verb{[0, Inf)}: Base node size.}

\item{edge_scale}{Numeric \verb{[0, Inf)}: Edge width multiplier.}

\item{node_opacity, edge_opacity}{Numeric \verb{[0, 1]}: Opacity of each.}

\item{palette}{Optional Character: Community colors, overriding the theme
palette. \code{NULL} uses the theme's.}

\item{node_color}{Character: Node color when not colored by group.}

\item{positive_color, negative_color}{Character: Edge colors by sign.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{origin}{Optional Named character: Where each value came from. Normally
computed from which arguments were supplied; pass it only when restoring a
config that already carries provenance.}

\item{writer}{Optional Named character: Which interface wrote the config.}
}
\value{
\link{NetworkConfig} object.
}
\description{
The seam between convenient input and a complete, validated object. \strong{Every
argument is optional}, which is what lets the published schema require
nothing.
}
\examples{
m <- matrix(
  c(0, 1, 1, 1, 0, 1, 1, 1, 0),
  nrow = 3,
  dimnames = list(letters[1:3], letters[1:3])
)
draw(setup_NetworkConfig(), data = m)
}
\author{
EDG
}
