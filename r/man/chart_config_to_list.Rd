% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_io.R
\name{chart_config_to_list}
\alias{chart_config_to_list}
\title{Convert a chart config to a plain list}
\usage{
chart_config_to_list(config, complete = FALSE)
}
\arguments{
\item{config}{\link{ChartConfig}: The chart configuration.}

\item{complete}{Logical: If TRUE, emit the output-config form: every
property, plus provenance.}
}
\value{
Named list, ready for \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}}.
}
\description{
Convert a chart config to a plain list
}
\details{
Two forms, matching the two the schema publishes:
\itemize{
\item \code{complete = FALSE} (default) drops unset properties, so an authored config
stays as small as it was authored. \code{type} is always kept: it carries the
document's shape and is the one key the schema requires.
\item \code{complete = TRUE} keeps every property, unset ones included, so they
serialize as explicit nulls -- and stamps the \code{writer}. A consumer of such
a document can read every key directly instead of reproducing this
package's defaults. Requires a full \code{origin} map; see \code{\link[=write_chart_config]{write_chart_config()}}.
}
}
\examples{
chart_config_to_list(setup_ScatterConfig(x = "wt", y = "mpg"))
}
\author{
EDG
}
