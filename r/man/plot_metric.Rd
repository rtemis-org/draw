% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_metric.R
\name{plot_metric}
\alias{plot_metric}
\title{Plot Stored Metrics Across Model Resamples}
\usage{
plot_metric(x, ...)
}
\arguments{
\item{x}{rtemis::SupervisedRes: Resampled fitted model.}

\item{...}{Additional arguments to \code{\link[=draw_metric]{draw_metric()}}.}

\item{what}{Character: \code{"all"} or unique \code{"training"} and/or \code{"test"} samples.}

\item{metric}{Optional Character: Stored metric name.}
}
\value{
htmlwidget: Metric distributions with all observations by default.
}
\description{
Draw-owned S7 generic for rtemis \code{SupervisedRes} results, including
regression and classification. Uses recorded per-resample metrics in
\code{resample_ids} order; list names do not change that producer contract.
The default metric is \code{balanced_accuracy} for classification and \code{rsq} for
regression. Classification selects overall metrics, not per-class scores.
}
\details{
All available training and test reports are selected by default. An
explicitly requested unavailable report is an error. Missing metric values
remain missing; a metric absent from every selected report is an error.
No predictions or aggregate means are used to reconstruct fold values.
Use \code{rtemis.draw::plot_metric()} when both packages are attached.
}
\examples{
\dontshow{if (requireNamespace("rtemis", quietly = TRUE)) withAutoprint(\{ # examplesIf}
model <- rtemis::train(mtcars[, c("wt", "mpg")],
  hyperparameters = rtemis::setup_GLM(),
  outer_resampling_config = rtemis::setup_KFold(n_resamples = 3L), execution_config = rtemis::setup_SerialExecution(), verbosity = 0L)
rtemis.draw::plot_metric(model)
\dontshow{\}) # examplesIf}
}
