% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_line.R
\name{LineConfig}
\alias{LineConfig}
\title{Line Chart Configuration}
\usage{
LineConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  x = NULL,
  y = NULL,
  blocks = NULL,
  smooth = FALSE,
  area = FALSE,
  points = TRUE,
  zoom = FALSE,
  palette = NULL,
  block_color = NULL,
  block_opacity = 0.2,
  pad = 0.04,
  xlim = NULL,
  ylim = NULL,
  xlab = NULL,
  ylab = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{x}{Optional Character: Column along the horizontal axis.}

\item{y}{Optional Character: Columns to plot, one line each.}

\item{blocks}{Optional Character: Column whose contiguous runs shade vertical
background bands.}

\item{smooth}{Logical: Draw smoothed lines.}

\item{area}{Logical: Fill the area under each line.}

\item{points}{Logical: Show a marker at each data value.}

\item{zoom}{Logical: Enable the zoom control.}

\item{palette}{Optional Character: Series colors, overriding the theme
palette for this chart. \code{NULL} uses the theme's.}

\item{block_color}{Optional Character: Band colors.}

\item{block_opacity}{Numeric \verb{[0, 1]}: Band opacity.}

\item{pad}{Numeric \verb{[0, Inf)}: Fraction of the data range to extend each axis
by when the limits are not given.}

\item{xlim, ylim}{Optional Numeric: Axis limits, length 2. \code{NULL} derives them
from the data, padded by \code{pad}.}

\item{xlab, ylab}{Optional Character: Axis labels. \code{NULL} derives them from
the data.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Plot margins in pixels.}
}
\value{
\code{LineConfig} object.
}
\description{
A serializable description of a line chart. Build one with
\code{\link[=setup_LineConfig]{setup_LineConfig()}} rather than calling this constructor directly.
}
\details{
\code{x} names the column along the horizontal axis. \code{y} names \strong{one or more}
columns, one line each, as \link{BarConfig} does.
}
\examples{
setup_LineConfig(x = "wt", y = "mpg")@type
}
\author{
EDG
}
