% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_bar.R
\name{BarConfig}
\alias{BarConfig}
\title{Bar Chart Configuration}
\usage{
BarConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  x = NULL,
  y = NULL,
  stack = FALSE,
  horizontal = FALSE,
  palette = NULL,
  xlab = NULL,
  ylab = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{x}{Optional Character: Column holding the categories.}

\item{y}{Optional Character: Columns holding the values, one series each.}

\item{stack}{Logical: If TRUE, stack the series instead of grouping them.}

\item{horizontal}{Logical: If TRUE, draw the bars horizontally.}

\item{palette}{Optional Character: Series colors, overriding the theme
palette for this chart. \code{NULL} uses the theme's.}

\item{xlab, ylab}{Optional Character: Axis labels. \code{NULL} derives them from
the data.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Plot margins in pixels.}
}
\value{
\code{BarConfig} object.
}
\description{
A serializable description of a bar chart. Build one with
\code{\link[=setup_BarConfig]{setup_BarConfig()}} rather than calling this constructor directly.
}
\details{
\code{x} names the column holding the categories. \code{y} names \strong{one or more}
columns, one series each -- a single column draws one set of bars, several
draw a grouped or stacked chart with the column names as the series names.
That is the difference from \link{ScatterConfig}, whose \code{x} and \code{y} each name
exactly one column.
}
\examples{
setup_BarConfig(x = "cyl", y = "mpg")@type
}
\author{
EDG
}
