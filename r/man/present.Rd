% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/present.R
\name{present}
\alias{present}
\title{Describe and Plot Fitted Models}
\usage{
present(x, ...)
}
\arguments{
\item{x}{rtemis model or List: Fitted ordinary/resampled models.}

\item{...}{Arguments for the selected presentation: \code{what}, \code{verbosity}, and
drawing arguments. Classification accepts \code{type = "roc"} or \code{"confusion"}.
Lists accept \code{metric}, \code{model_names}, \code{what}, \code{ylim}, \code{boxpoints},
\code{quartiles}, \code{whisker}, \code{palette}, \code{theme}, \code{width}, \code{height}, and \code{filename}.
Single models forward drawing arguments to their plot_* method. Comparison
lists use training/test samples; individual ordinary models also support
validation. Explicit \code{ylim} is a finite increasing pair for comparisons.}
}
\value{
htmlwidget: One chart or one complete \code{\link[=draw_panels]{draw_panels()}} figure.
}
\description{
Draw-owned S7 generic, separate from \code{rtemis::present()}. Ordinary regression
presents true/predicted values; ordinary classification presents ROC curves
or confusion panels. Resampled models present stored metric distributions.
Descriptions use \code{rtemis::describe()} unless \code{verbosity = 0}.
}
\details{
A nonempty homogeneous list compares models of the same task and sampling
kind. Ordinary models use grouped metric bars. Resampled models use one
boxplot panel per sample with common value limits and aligned model labels.
Fold counts may differ; fold scores are neither paired nor pooled across
models. Missing scores stay missing. Wholly unavailable samples are omitted
under \code{what = "all"}; explicit unavailable samples are errors.

Comparison labels use \code{model_names}, complete list names, or algorithm
names, made unique in that order. The existing rtemis data-fingerprint
mismatch notice is retained, including when descriptions are disabled.
}
\examples{
\dontshow{if (requireNamespace("rtemis", quietly = TRUE)) withAutoprint(\{ # examplesIf}
model <- rtemis::train(mtcars[, c("wt", "mpg")],
  hyperparameters = rtemis::setup_GLM(), verbosity = 0L)
rtemis.draw::present(model, verbosity = 0L)
\dontshow{\}) # examplesIf}
}
