% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_scatter.R
\name{ScatterConfig}
\alias{ScatterConfig}
\title{Scatter Chart Configuration}
\usage{
ScatterConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  x = NULL,
  y = NULL,
  size = NULL,
  group = NULL,
  fit = NULL,
  se = TRUE,
  n_fit = 200L,
  fit_alpha = 0.25,
  palette = NULL,
  square = FALSE,
  equal_axes = FALSE,
  pad = 0.04,
  xlim = NULL,
  ylim = NULL,
  xlab = NULL,
  ylab = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{x, y}{Optional Character: Columns drawn on each axis.}

\item{size}{Optional Character: Column giving per-point size.}

\item{group}{Optional Character: Column to group and color points by.}

\item{fit}{Optional Character \{"glm", "gam"\}: Fit to overlay. \code{NULL} draws
no fit.}

\item{se}{Logical: If TRUE, shade the fit standard-error band.}

\item{n_fit}{Integer \verb{[2, Inf)}: Points used to draw the fit line.}

\item{fit_alpha}{Numeric \verb{[0, 1]}: Opacity of the standard-error band.}

\item{palette}{Optional Character: Series colors, overriding the theme
palette for this chart. \code{NULL} uses the theme's.}

\item{square}{Logical: If TRUE, draw the plotting box square -- equal height
and width in pixels, excluding axis labels and margins.}

\item{equal_axes}{Logical: If TRUE, give one data unit the same size in
pixels on both axes. Set with \code{square} for a plot that is both, such as a
true-versus-predicted plot whose identity line runs at 45 degrees; the two
axes are then made to span the same interval.}

\item{pad}{Numeric \verb{[0, Inf)}: Fraction of the data range to extend each axis
by when \code{xlim} / \code{ylim} are not given. The default matches base R's
\code{xaxs = "r"}, which extends the range by 4\% at each end.}

\item{xlim, ylim}{Optional Numeric: Axis limits, length 2. \code{NULL} derives them
from the data, padded by \code{pad}.}

\item{xlab, ylab}{Optional Character: Axis labels. \code{NULL} derives them from
the data.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Plot margins in pixels.}
}
\value{
\code{ScatterConfig} object.
}
\description{
A serializable description of a scatter chart: which columns it binds, its
semantics, and its appearance. Build one with \code{\link[=setup_ScatterConfig]{setup_ScatterConfig()}} rather
than calling this constructor directly.
}
\details{
The data-binding properties (\code{x}, \code{y}, \code{size}, \code{group}) hold \strong{column names},
not values. The values come from the \code{data} argument to \code{\link[=draw]{draw()}}, or from the
inherited \code{dat_path}.

Margins are declared as four scalars rather than one named vector: that is
what states cleanly in a schema, while \code{\link[=draw_scatter]{draw_scatter()}} keeps the convenient
\code{margins} vector. Sides left \code{NULL} fall back to the chart's own layout.
}
\examples{
cfg <- setup_ScatterConfig(x = "wt", y = "mpg", fit = "glm")
cfg@type
}
\author{
EDG
}
