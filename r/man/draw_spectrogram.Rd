% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_spectrogram.R
\name{draw_spectrogram}
\alias{draw_spectrogram}
\title{Draw a Spectrogram}
\usage{
draw_spectrogram(
  x,
  sample_rate = NULL,
  time = NULL,
  frequency = NULL,
  n_fft = 256L,
  window = "hann",
  overlap = NULL,
  power = TRUE,
  db = TRUE,
  db_range = 80,
  freq_scale = "linear",
  freq_range = NULL,
  freq_unit = "Hz",
  time_range = NULL,
  time_unit = "s",
  palette = "magma",
  palette_reverse = FALSE,
  n_colors = 256L,
  zlim = NULL,
  show_colorbar = TRUE,
  colorbar_title = NULL,
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  theme = NULL,
  margins = NULL,
  width = NULL,
  height = NULL,
  filename = NULL
)
}
\arguments{
\item{x}{Numeric matrix (freq x time) or numeric vector (raw signal).
A matrix is used directly; \code{time} and \code{frequency} vectors supply axis
values (defaults to sample indices when absent). A complex matrix (raw
STFT output, e.g. from \code{\link[signal:specgram]{signal::specgram()}}\code{$S}) is also accepted.
A vector triggers STFT computation via \code{\link[signal:specgram]{signal::specgram()}}; \code{sample_rate}
is required.}

\item{sample_rate}{Optional Numeric \verb{(0, Inf)}: Sampling frequency in Hz.
Required when \code{x} is a raw signal vector.}

\item{time}{Optional Numeric: Time axis values in seconds, length \code{ncol(x)}.
Only used when \code{x} is a matrix.}

\item{frequency}{Optional Numeric: Frequency axis values in Hz, length
\code{nrow(x)}. Only used when \code{x} is a matrix.}

\item{n_fft}{Integer \verb{[2, Inf)}: FFT window size in samples. Passed to
\code{\link[signal:specgram]{signal::specgram()}} as \code{n}. Only used when \code{x} is a raw signal.}

\item{window}{Character \{"hann", "hamming", "blackman", "bartlett",
"rectangular"\} or Numeric: Window function name or a pre-built window
vector. Passed to \code{\link[signal:specgram]{signal::specgram()}}. Only used when \code{x} is a raw signal.}

\item{overlap}{Optional Integer \verb{[0, n_fft)}: Overlap between consecutive
frames in samples. Defaults to \code{n_fft / 2}. Only used when \code{x} is a raw
signal.}

\item{power}{Logical: Treat spectral values as power (\code{TRUE}) or amplitude
(\code{FALSE}). When \code{x} is complex, controls whether the STFT magnitude is
squared (\verb{|S|^2}) or left as-is (\verb{|S|}). When \code{db = TRUE}, also determines
the dB scaling for real matrices: \code{10 * log10()} for power, \code{20 * log10()}
for amplitude.}

\item{db}{Logical: Convert to dB. For power: \code{10 * log10()}; for amplitude:
\code{20 * log10()}. Set \code{FALSE} when passing a pre-computed dB matrix.}

\item{db_range}{Numeric \verb{(0, Inf)}: Dynamic range to display in dB below the
spectral peak. Values below \code{peak - db_range} are clipped to the floor.}

\item{freq_scale}{Character \{"linear", "log"\}: Frequency axis scale. With
\code{"log"} the DC component (0 Hz) is automatically dropped.}

\item{freq_range}{Optional Numeric[2]: Frequency range to display in Hz,
e.g. \code{c(20, 8000)}. Applied after STFT computation.}

\item{freq_unit}{Character \{"Hz", "kHz"\}: Unit for the frequency axis.}

\item{time_range}{Optional Numeric[2]: Time range to display in seconds.}

\item{time_unit}{Character \{"s", "ms"\}: Unit for the time axis.}

\item{palette}{Character: Colour palette. Accepts a \pkg{viridisLite} palette
name (\code{"magma"} (default), \code{"inferno"}, \code{"plasma"},
\code{"viridis"}, \code{"cividis"}, \code{"mako"}, \code{"rocket"}, \code{"turbo"}), \code{"diverging"}
for the rtemis teal-background-orange scale (suitable for signed data
such as EEG/MEG amplitudes), or a character vector of >= 2 hex colours for a
custom ramp.}

\item{palette_reverse}{Logical: Reverse the palette direction.}

\item{n_colors}{Integer \verb{[2, Inf)}: Number of discrete colours in the
generated palette.}

\item{zlim}{Optional Numeric[2]: Colour-scale limits after all
transformations (dB clipping, unit conversion). Defaults to the data range.}

\item{show_colorbar}{Logical: Show the continuous visual-map colorbar.}

\item{colorbar_title}{Optional Character: Colorbar label. Default: \code{"dB"},
\code{"Power"}, or \code{"Amplitude"} derived from \code{db} and \code{power}.}

\item{title}{Optional Character: Chart title.}

\item{xlab}{Optional Character: X-axis label. Default: \code{"Time (s)"} or
\code{"Time (ms)"} depending on \code{time_unit}.}

\item{ylab}{Optional Character: Y-axis label. Default: \code{"Frequency (Hz)"}
or \code{"Frequency (kHz)"} depending on \code{freq_unit}.}

\item{theme}{Optional \link{Theme}, list, or \code{NA}: Theme override passed to
\code{\link[=draw]{draw()}}.}

\item{margins}{Optional Named numeric or character vector / list: Plot
margins in pixels. Valid names: \code{"top"}, \code{"right"}, \code{"bottom"}, \code{"left"}.}

\item{width}{Optional Numeric or Character: Widget width.}

\item{height}{Optional Numeric or Character: Widget height.}

\item{filename}{Optional Character: If provided, the widget is saved via
\code{\link[=save_drawing]{save_drawing()}}.}
}
\value{
htmlwidget
}
\description{
Renders an interactive time-frequency spectrogram as an ECharts heatmap
widget. Accepts either a raw signal vector (STFT is computed internally via
\code{\link[signal:specgram]{signal::specgram()}}) or a pre-computed spectrogram matrix (freq x time).
}
\details{
Corresponds to \code{HeatmapSeriesOption} in \code{src/chart/heatmap/HeatmapSeries.ts}.
ECharts docs: \url{https://echarts.apache.org/en/option.html#series-heatmap}
}
\examples{
if (requireNamespace("signal", quietly = TRUE)) {
  t_vec <- seq(0, 2, by = 1 / 8000)
  sig   <- signal::chirp(t_vec, 200, 2, 2000)
  draw_spectrogram(sig, sample_rate = 8000)
}
}
