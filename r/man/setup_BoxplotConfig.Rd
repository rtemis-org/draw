% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_boxplot.R
\name{setup_BoxplotConfig}
\alias{setup_BoxplotConfig}
\title{Set up a Boxplot Configuration}
\usage{
setup_BoxplotConfig(
  x = NULL,
  group = NULL,
  labels = NULL,
  horizontal = FALSE,
  na_rm = TRUE,
  observation = NULL,
  quartiles = "linear",
  whisker = 1.5,
  boxpoints = "none",
  point_size = 5,
  point_alpha = 0.6,
  point_spread = 0.5,
  palette = NULL,
  fill_alpha = 0.25,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL,
  dat_path = NULL,
  origin = NULL,
  writer = NULL
)
}
\arguments{
\item{x}{Optional Character: Columns to summarize, one box each.}

\item{group}{Optional Character: Column that splits each box by level.}

\item{labels}{Optional Character: Box labels. \code{NULL} uses the bound column
names.}

\item{horizontal}{Logical: Draw the boxes horizontally.}

\item{na_rm}{Logical: Remove missing values. FALSE rejects missing input.}

\item{observation}{Optional Character: Column identifying observations in point tooltips.}

\item{quartiles}{Character {"linear", "hinges"}: Quartile convention.}

\item{whisker}{Numeric \verb{[0, Inf)}: Finite IQR fence multiplier;
zero uses full-range whiskers.}

\item{boxpoints}{Character {"none", "all", "outliers"}: Values to overlay.}

\item{point_size}{Numeric \verb{(0, Inf)}: Finite point diameter in pixels.}

\item{point_alpha}{Numeric \verb{[0, 1]}: Point opacity.}

\item{point_spread}{Numeric \verb{[0, 1]}: Fraction of box width occupied by offsets.}

\item{palette}{Optional Character: Box colors, overriding the theme palette
for this chart. \code{NULL} uses the package's box colors.}

\item{fill_alpha}{Numeric \verb{[0, 1]}: Box fill opacity.}

\item{xlab, ylab}{Optional Character: Axis labels.}

\item{title}{Optional Character: Chart title.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Plot margins in pixels.}

\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{origin}{Optional Named character: Where each value came from. Normally
computed from which arguments were supplied; pass it only when restoring a
config that already carries provenance.}

\item{writer}{Optional Named character: Which interface wrote the config.}
}
\value{
\link{BoxplotConfig} object.
}
\description{
The seam between convenient input and a complete, validated object. \strong{Every
argument is optional}, which is what lets the published schema require
nothing.
}
\examples{
draw(setup_BoxplotConfig(x = c("mpg", "hp")), data = mtcars)
}
\author{
EDG
}
