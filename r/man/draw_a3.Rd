% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_a3.R
\name{draw_a3}
\alias{draw_a3}
\title{Draw an A3 Amino Acid Sequence Visualization}
\usage{
draw_a3(
  x,
  n_per_row = 21L,
  residue_spacing = 0.3,
  marker_size = 28,
  font_size = 18,
  line_width = 2,
  show_markers = TRUE,
  show_labels = TRUE,
  position_every = 10L,
  region_opacity = 0.35,
  ptm_placement = "radial",
  residue_fill = "#E7E5E4",
  residue_stroke = "#44403C",
  label_color = "#1C1917",
  pos_label_color = "#78716C",
  variant_color = "#FA6E1E",
  disease_variant_color = "#E266AE",
  enable_zoom = TRUE,
  title = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  filename = NULL
)
}
\arguments{
\item{x}{\code{A3} object from \code{\link[rtemis.a3:create_A3]{rtemis.a3::create_A3()}} or
\code{\link[rtemis.a3:read_A3json]{rtemis.a3::read_A3json()}}.}

\item{n_per_row}{Integer \verb{[2, Inf)}: Number of residues per wrapped row.}

\item{residue_spacing}{Numeric \verb{(0, Inf)}: Horizontal spacing multiplier.
Values below \code{1} compress the layout; \code{1} gives natural hex spacing.}

\item{marker_size}{Numeric \verb{(0, Inf)}: Residue circle diameter in pixels.}

\item{font_size}{Numeric \verb{(0, Inf)}: Base font size in pixels for residue
and position labels.}

\item{line_width}{Numeric \verb{(0, Inf)}: Backbone line width in pixels.}

\item{show_markers}{Logical: Whether to render the backbone line and
residue circles.}

\item{show_labels}{Logical: Whether to render single-letter residue labels.}

\item{position_every}{Optional Integer \verb{[1, Inf)}: Show a numeric position
label every N residues. \code{NULL} disables position labels.}

\item{region_opacity}{Numeric \verb{[0, 1]}: Opacity of region band overlays.}

\item{ptm_placement}{Character \verb{\{"radial", "innerRadial", "outerRadial"\}}:
Placement of PTM symbols relative to the residue circle.
\code{"radial"} centres on the circle edge; \code{"innerRadial"} places inside;
\code{"outerRadial"} places outside.}

\item{residue_fill}{Character: Residue circle fill color.}

\item{residue_stroke}{Character: Residue circle stroke and backbone line color.}

\item{label_color}{Character: Default residue label text color.}

\item{pos_label_color}{Character: Position label text color.}

\item{variant_color}{Character: Label color for variant residues.}

\item{disease_variant_color}{Character: Label color for disease-associated
variant residues (takes precedence over \code{variant_color}).}

\item{enable_zoom}{Logical: Whether to enable Shift+scroll zoom.}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override. \code{NULL} auto-detects
light/dark mode; \code{NA} uses raw ECharts defaults.}

\item{width}{Optional Numeric or Character: Widget width.}

\item{height}{Optional Numeric or Character: Widget height. Auto-computed
from layout bounds and \code{marker_size} when \code{NULL}.}

\item{filename}{Optional Character: If provided, save the widget to this
file via \code{\link[=save_drawing]{save_drawing()}}.}
}
\value{
htmlwidget: Widget object.
}
\description{
Renders an \code{A3} object (from \code{rtemis.a3}) as an interactive ECharts
amino-acid sequence diagram. The sequence is wrapped into rows in a
meander/serpentine path, with optional site, region, PTM, processing, and
variant annotations overlaid as distinct series and collected in a
vertical legend.
}
\details{
Corresponds to \code{createA3EChartsOption()} in
\code{src/lib/a3/visualization/echarts.ts}.
}
\examples{
if (requireNamespace("rtemis.a3", quietly = TRUE)) {
  a <- rtemis.a3::create_A3(
    "MAEPRQEFEVMEDHAGTYGLGDRK",
    site = list(
      Active_site = rtemis.a3::annotation_position(c(5L, 17L))
    ),
    region = list(
      Domain = rtemis.a3::annotation_range(
        matrix(c(3L, 10L, 15L, 22L), ncol = 2, byrow = TRUE)
      )
    )
  )
  draw_a3(a)
}
}
