% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_metric.R
\name{draw_metric}
\alias{draw_metric}
\title{Draw a Metric's Distribution Across Resamples}
\usage{
draw_metric(
  data,
  metric = NULL,
  boxpoints = "all",
  horizontal = FALSE,
  xlab = if (horizontal) labelify(metric) else NULL,
  ylab = if (!horizontal) labelify(metric) else NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL,
  filename = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame: Long records with character \code{fold}, \code{split}, and
\code{metric} identities, and numeric \code{value} scores (finite or NA).}

\item{metric}{Optional Character: Metric to select. May be omitted when the
table contains one metric.}

\item{boxpoints}{Character {"none", "all", "outliers"}: Values to overlay.}

\item{horizontal}{Logical: Draw horizontal distributions.}

\item{xlab, ylab}{Optional Character: Axis labels. The value axis derives its
label from the selected metric when omitted; explicit NULL omits it.}

\item{theme}{Optional \link{Theme}: Theme override. The palette inside the theme can be
overridden per-chart with the \code{color} argument.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{element_id}{Optional Character: Explicit DOM element ID for the widget
container. \code{NULL} lets htmlwidgets generate one.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}

\item{...}{Additional arguments to \code{\link[=setup_BoxplotConfig]{setup_BoxplotConfig()}}, such as
\code{quartiles}, \code{whisker}, \code{point_spread}, \code{palette}, and \code{title}.}
}
\value{
htmlwidget: Metric distributions with optional point overlays.
}
\description{
Draw stored fold scores using \link{BoxplotConfig}, with every available
observation overlaid. Fold scores carry equal weight; this is a distribution
of recorded scores, not a metric recomputed from pooled predictions or a
confidence interval. Negative and unbounded metrics such as R-squared are
not clipped to the unit interval.
}
\details{
Select one metric per chart. Sample and fold order follow first appearance.
Absent sample/fold pairs and explicit NA scores remain missing; available
values alone define each box. Entirely unavailable samples retain an empty
category, and missing values are disclosed in a caption. Entirely
unavailable input is an error. See \code{\link[=draw_boxplot]{draw_boxplot()}} for exact quartile,
whisker, and point-placement semantics.

The portable result uses one numeric column per split and one observation
identifier column, bound through \code{\link[=setup_BoxplotConfig]{setup_BoxplotConfig()}}. No fitted object,
R callback, or attribute is needed to render it in another interface.
}
\examples{
scores <- data.frame(fold = rep(c("Fold1", "Fold2", "Fold3"), 2),
  split = rep(c("Training", "Test"), each = 3), metric = "rsq",
  value = c(.9, .85, .8, .7, .65, .6))
draw_metric(scores)
}
