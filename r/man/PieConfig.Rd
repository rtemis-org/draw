% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_pie.R
\name{PieConfig}
\alias{PieConfig}
\title{Pie Chart Configuration}
\usage{
PieConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  values = NULL,
  labels = NULL,
  rose_type = NULL,
  radius = "75\%",
  palette = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{values}{Optional Character: Column holding the slice values.}

\item{labels}{Optional Character: Column holding the slice labels.}

\item{rose_type}{Optional Character \{"radius", "area"\}: Draw as a
Nightingale rose chart, sizing slices by radius or by area. \code{NULL} draws a
plain pie.}

\item{radius}{Character: Outer radius, as a CSS length or percentage.}

\item{palette}{Optional Character: Slice colors, overriding the theme
palette for this chart. \code{NULL} uses the theme's.}
}
\value{
\code{PieConfig} object.
}
\description{
A serializable description of a pie chart. Build one with
\code{\link[=setup_PieConfig]{setup_PieConfig()}} rather than calling this constructor directly.
}
\examples{
setup_PieConfig(values = "n", labels = "kind")@type
}
\author{
EDG
}
