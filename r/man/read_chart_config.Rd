% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_io.R
\name{read_chart_config}
\alias{read_chart_config}
\title{Read a chart config from JSON}
\usage{
read_chart_config(x)
}
\arguments{
\item{x}{Character: Path to a JSON file, or a JSON string.}
}
\value{
\link{ChartConfig} subclass object.
}
\description{
Reconstructs through the chart's \verb{setup_*} function rather than its
constructor, so a document from any source -- another interface, a hand-written
file, a language model -- arrives resolved through the same seam a direct call
goes through.
}
\examples{
path <- tempfile(fileext = ".json")
write_chart_config(setup_ScatterConfig(x = "wt", y = "mpg", fit = "glm"), path)
read_chart_config(path)@fit
unlink(path)
}
\author{
EDG
}
