% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_heatmap.R
\name{HeatmapConfig}
\alias{HeatmapConfig}
\title{Heatmap Configuration}
\usage{
HeatmapConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  row_names = NULL,
  col_names = NULL,
  triangle = NULL,
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  dist_method = "euclidean",
  hclust_method = "complete",
  square_cells = NULL,
  show_row_dendro = TRUE,
  show_col_dendro = TRUE,
  dendro_row_width = 60,
  dendro_col_height = 60,
  dendro_color = NULL,
  dendro_uniform = FALSE,
  dendro_row_side = "right",
  dendro_col_side = "top",
  colormap = NULL,
  zlim = NULL,
  show_values = FALSE,
  value_digits = 2L,
  show_colorbar = TRUE,
  colorbar_orient = "vertical",
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{row_names, col_names}{Optional Character: Labels overriding the matrix
dimnames.}

\item{triangle}{Optional Character \{"lower", "upper"\}: Show only one
triangle, for a symmetric matrix.}

\item{cluster_rows, cluster_cols}{Logical: Reorder by hierarchical clustering.}

\item{dist_method}{Character: Distance measure for clustering.}

\item{hclust_method}{Character: Linkage method for clustering.}

\item{square_cells}{Optional Logical: Force square cells, sizing the
container to match.}

\item{show_row_dendro, show_col_dendro}{Logical: Draw each dendrogram.}

\item{dendro_row_width, dendro_col_height}{Numeric \verb{[0, Inf)}: Dendrogram
panel size in pixels.}

\item{dendro_color}{Optional Character: Dendrogram line color.}

\item{dendro_uniform}{Logical: Draw dendrograms with uniform branch heights.}

\item{dendro_row_side}{Character \{"left", "right"\}: Row dendrogram side.}

\item{dendro_col_side}{Character \{"top", "bottom"\}: Column dendrogram side.}

\item{colormap}{Optional Character: Colors defining the continuous scale.
\code{NULL} derives a theme-aware diverging or sequential scale.}

\item{zlim}{Optional Numeric: Color-scale limits, length 2.}

\item{show_values}{Logical: Print each cell's value.}

\item{value_digits}{Integer \verb{[0, Inf)}: Digits for printed values.}

\item{show_colorbar}{Logical: Draw the color bar.}

\item{colorbar_orient}{Character \{"vertical", "horizontal"\}: Color bar
orientation.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Override the auto-computed margins, per side.}
}
\value{
\code{HeatmapConfig} object.
}
\description{
A serializable description of a heatmap. Build one with
\code{\link[=setup_HeatmapConfig]{setup_HeatmapConfig()}} rather than calling this constructor directly.
}
\details{
The bound data is a numeric matrix. \code{row_names} and \code{col_names} override the
matrix's own dimnames rather than naming columns to read.
}
\examples{
setup_HeatmapConfig(cluster_rows = TRUE)@type
}
\author{
EDG
}
