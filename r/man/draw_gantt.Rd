% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_gantt.R
\name{draw_gantt}
\alias{draw_gantt}
\title{Draw a Timeline / Gantt Chart}
\usage{
draw_gantt(
  tasks,
  group = NULL,
  axis_type = "value",
  bar_height = 0.6,
  bar_radius = 0,
  guides = TRUE,
  zoom = TRUE,
  tooltip = NULL,
  border = NULL,
  border_color = "#E53935",
  border_width = 1.5,
  xlab = NULL,
  title = NULL,
  color = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  filename = NULL
)
}
\arguments{
\item{tasks}{Tabular data (data.frame, data.table, or tibble): One row per
task bar. Must contain columns \code{label} (row / category), \code{start}, and
\code{end}. Repeated \code{label} values place multiple bars on the same row.}

\item{group}{Optional Character: Name of a column in \code{tasks} whose values
color the bars and produce a legend. When \code{NULL}, all bars share one color.}

\item{axis_type}{Character \{"value", "time"\}: Type of the x-axis. Use
\code{"value"} for numeric offsets (e.g. milliseconds from start) and \code{"time"}
for absolute timestamps; \code{POSIXct}/\code{Date} \code{start}/\code{end} columns are
converted to epoch milliseconds automatically.}

\item{bar_height}{Numeric \verb{(0, 1]}: Bar thickness as a fraction of one
category band.}

\item{bar_radius}{Numeric \verb{[0, Inf)}: Bar corner radius in pixels.}

\item{guides}{Logical: If \code{TRUE}, show an interactive axis pointer -- a guide
line that follows the mouse and labels the time value on the x-axis.}

\item{zoom}{Logical: If \code{TRUE}, enable interactive zoom -- mouse-wheel to zoom
and drag to pan on both the time and row axes (\code{inside} dataZoom), plus a
top-right toolbox with box-zoom, undo, and reset controls.}

\item{tooltip}{Optional Character: Name of a column in \code{tasks} to show as the
tooltip text for each bar. When \code{NULL}, a default \code{label: start - end}
tooltip is shown.}

\item{border}{Optional Character: Name of a logical column in \code{tasks}; bars
whose value is \code{TRUE} get an outline (in \code{border_color}) without changing
their fill -- e.g. to flag failures while the fill still encodes the group.}

\item{border_color}{Character: Outline color for bars flagged by \code{border}.}

\item{border_width}{Numeric \verb{[0, Inf)}: Outline width in pixels.}

\item{xlab}{Optional Character: x-axis label.}

\item{title}{Optional Character: Chart title.}

\item{color}{Optional Character: Color palette as a single color or character
vector overriding the theme palette. Groups are colored in order.}

\item{theme}{Optional \link{Theme}: Theme override.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{filename}{Optional Character: If provided, save the widget to this file
via \code{\link[=save_drawing]{save_drawing()}}.}
}
\value{
htmlwidget: Widget object.
}
\description{
Draw a timeline (Gantt) chart: one horizontal bar per task, positioned by
\code{start} and \code{end} on a value or time x-axis and grouped into rows by \code{label}
on the y-axis. Implemented as an ECharts \code{custom} series (ECharts has no
native Gantt series).
}
\examples{
tasks <- data.frame(
  label = c("load", "clean", "train", "predict"),
  start = c(0, 12, 30, 95),
  end = c(12, 30, 95, 100),
  status = c("ok", "ok", "ok", "error")
)
draw_gantt(tasks, group = "status")
}
\author{
EDG
}
