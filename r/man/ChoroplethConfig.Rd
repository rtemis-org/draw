% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_choropleth.R
\name{ChoroplethConfig}
\alias{ChoroplethConfig}
\title{Choropleth Map Configuration}
\usage{
ChoroplethConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  location = NULL,
  value = NULL,
  tooltip = NULL,
  resolution = "country",
  classification = "quantile",
  num_classes = 5,
  colormap = "blues",
  opacity = 1,
  show_boundaries = TRUE,
  outline_width = 0.2,
  show_legend = TRUE,
  legend_position = "bottom-right",
  tooltip_position = "top-right",
  report_position = "bottom-left",
  value_label = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{location}{Optional Character: Column holding the region identifiers.}

\item{value}{Optional Character: Column holding the value to shade by.}

\item{tooltip}{Optional Character: Column holding per-region tooltip text.}

\item{resolution}{Character \{"country", "state", "county"\}: Which boundary
set the locations refer to.}

\item{classification}{Character \{"quantile", "equal", "jenks"\}: How values
are binned into classes.}

\item{num_classes}{Numeric \verb{[2, 12]}: Number of color classes.}

\item{colormap}{Character: Continuous color scheme for the classes.}

\item{opacity}{Numeric \verb{[0, 1]}: Fill opacity.}

\item{show_boundaries, show_legend}{Logical: Whether to draw each.}

\item{outline_width}{Numeric \verb{[0, Inf)}: Boundary line width.}

\item{legend_position, tooltip_position, report_position}{Character: Corner
each overlay is anchored to.}

\item{value_label}{Optional Character: Label for the value in the legend and
tooltip. \code{NULL} derives it from the bound column.}
}
\value{
\code{ChoroplethConfig} object.
}
\description{
A serializable description of a choropleth map. Build one with
\code{\link[=setup_ChoroplethConfig]{setup_ChoroplethConfig()}} rather than calling this constructor directly.
}
\examples{
setup_ChoroplethConfig(location = "st", value = "v")@type
}
\author{
EDG
}
