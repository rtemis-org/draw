% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_learning_curve.R
\name{draw_learning_curve}
\alias{draw_learning_curve}
\title{Draw a Learning Curve from Recorded Losses}
\usage{
draw_learning_curve(
  data,
  unit = attr(data, "unit", exact = TRUE),
  selected = attr(data, "selected", exact = TRUE),
  xlab = NULL,
  ylab = "Loss",
  title = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL,
  filename = NULL,
  ...,
  legend_position = "top",
  legend_placement = "outside"
)
}
\arguments{
\item{data}{Data frame: Numeric \code{iteration} and at least one of
\code{loss_training} or \code{loss_validation}. An optional \code{tree} column identifies
ensemble members. Iterations must be unique within each tree.}

\item{unit}{Optional Character: Unit of progress, such as \code{"epochs"} or
\code{"leaves"}. Read from the table's \code{unit} attribute when omitted. NULL uses
\code{"Iteration"} for the axis label.}

\item{selected}{Optional Numeric: Selected step. Read from the table's
\code{selected} attribute when omitted. NA or NULL means no selected step.}

\item{xlab}{Optional Character: Horizontal axis label, overriding \code{unit}.}

\item{ylab}{Optional Character: Vertical axis label.}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override. Set \code{palette} in \code{...} to
override its series colors.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{element_id}{Optional Character: Explicit DOM element ID for the widget
container. \code{NULL} lets htmlwidgets generate one.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}

\item{...}{Additional appearance and axis arguments to \code{\link[=setup_LineConfig]{setup_LineConfig()}},
such as \code{palette}, \code{zoom}, \code{points}, \code{xlim}, and \code{margin_top}. The data
bindings are determined by the loss table.}

\item{legend_position}{Character \{"top", "bottom", "left", "right",
"top-left", "top-right", "bottom-left", "bottom-right"\}: Legend anchor.
Top/bottom anchors use horizontal rows; left/right anchors use a vertical
column. Corner anchors align within the top or bottom row.}

\item{legend_placement}{Character \{"outside", "inside"\}: Relation to the
plotting area. Outside placement reserves space for the complete legend;
inside placement overlays the data. Neither setting adds a missing legend.}
}
\value{
htmlwidget: ECharts learning curve.
}
\description{
Draw training and validation losses against training progress using a
\link{LineConfig}. This function accepts an ordinary table and does not require
rtemis. Use \code{\link[=plot_learning]{plot_learning()}} to draw the curve of a model.
}
\details{
Steps are sorted numerically. With a \code{tree} column, losses are averaged at
each step over trees with a nonmissing loss for that series. Trees that have
stopped contribute no value at later steps. Entirely missing series are
omitted; missing values within a series remain gaps.

The selected point uses training loss if that series exists, otherwise
validation loss. An absent step or missing loss produces no selected marker.
\code{selected = NULL} disables it. The marker follows the line config's \code{points}
setting, so \code{points = FALSE} hides it along with the other point symbols.

For another interface, materialize columns \code{iteration}, \code{Training},
\code{Validation}, and optionally \code{Selected} (missing except at the selected
step), omitting unavailable loss columns. Bind these with \code{\link[=setup_LineConfig]{setup_LineConfig()}}
using \code{x = "iteration"} and \code{y} naming the loss columns. Store the unit as
\code{xlab}. That data and config reproduce the chart without R attributes or a
fitted model; aggregation is performed before rendering.
}
\examples{
losses <- data.frame(
  iteration = 1:4,
  loss_training = c(4, 2, 1, 0.5),
  loss_validation = c(4.5, 2.5, 2, 2.2)
)
draw_learning_curve(losses, unit = "epochs", selected = 3)
}
