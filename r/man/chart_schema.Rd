% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{chart_schema}
\alias{chart_schema}
\title{Generate a JSON Schema for a chart config class}
\usage{
chart_schema(cls, id, title, description, complete = FALSE)
}
\arguments{
\item{cls}{S7 class: A \link{ChartConfig} subclass.}

\item{id}{Character: The schema's \verb{$id} URL.}

\item{title}{Character: Human-readable schema title.}

\item{description}{Character: What this chart is.}

\item{complete}{Logical: If TRUE, emit the output-config kind, requiring
every property.}
}
\value{
Named list: The JSON Schema, ready for \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}}.
}
\description{
Emits the schema for one \link{ChartConfig} subclass: every property it declares
or inherits, typed from its \code{PropertySpec}, plus a constant for the \code{type}
discriminator. The result is self-contained -- it validates a chart document
on its own, without the family dispatcher.
}
\details{
Two kinds, differing only in what they require:
\itemize{
\item \code{complete = FALSE} (an \strong{input config}) requires only \code{type}. The author
sets a subset and the interface fills in the rest.
\item \code{complete = TRUE} (an \strong{output config}) requires every property, so a
document that claims to be complete can be checked rather than trusted.
}

Neither emits a \code{default}: what an interface fills in is not a fact about
the document. See \code{plan/draw-schemas.md}.
}
\examples{
schema <- chart_schema(
  ScatterConfig,
  id = "https://schema.rtemis.org/chart/scatter/v1/schema.json",
  title = "rtemis ScatterConfig",
  description = "Scatter chart."
)
names(schema[["properties"]])
}
\author{
EDG
}
