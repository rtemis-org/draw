% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_varimp.R
\name{plot_varimp}
\alias{plot_varimp}
\title{Plot a Model's Variable Importance}
\usage{
plot_varimp(x, ...)
}
\arguments{
\item{x}{\code{rtemis::Supervised} or \code{rtemis::SupervisedRes}: Fitted model result.}

\item{...}{Additional arguments to \code{\link[=draw_varimp]{draw_varimp()}}, including \code{measure},
\code{top_n}, \code{rank_by}, \code{summary}, \code{absent}, and \code{title}. Fold IDs come from
the model and cannot be overridden.}
}
\value{
htmlwidget: ECharts variable-importance bars.
}
\description{
Draw importance recorded by a rtemis \code{Supervised} or \code{SupervisedRes} object
using \code{\link[=draw_varimp]{draw_varimp()}}. Use \code{rtemis.draw::plot_varimp()} when both packages
are attached; this draw-owned S7 generic is separate from rtemis's generic.
}
\details{
Named measures and variable identities are preserved. Resampled records
retain their fold IDs before summarizing. The first measure in the first
available fold is selected when \code{measure} is omitted; columns missing in
other folds are unavailable scores. The full fold list is passed to the
renderer, including folds with no importance result.

CART's \code{importance} and LightGBM's \code{Gain}, \code{Cover}, and \code{Frequency} come
from sparse split summaries: omitted variables in a reporting fold have
zero importance. These measures use \code{absent = "zero"}. Other measures use
\code{absent = "missing"}; pass \code{absent} explicitly to declare another contract.
Explicit NA values and wholly unavailable folds are always kept missing.

Models without any importance records produce an informative error.
The default title names the algorithm; \code{title = NULL} omits it.
See \code{\link[=draw_varimp]{draw_varimp()}} for selection, ranking, and missing-score semantics.
}
\examples{
\dontshow{if (requireNamespace("rtemis", quietly = TRUE)) withAutoprint(\{ # examplesIf}
model <- rtemis::train(
  mtcars[, c("wt", "hp", "mpg")],
  hyperparameters = rtemis::setup_GLM(), verbosity = 0L
)
rtemis.draw::plot_varimp(model)
\dontshow{\}) # examplesIf}
}
