% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_spectrogram.R
\name{setup_SpectrogramConfig}
\alias{setup_SpectrogramConfig}
\title{Set up a Spectrogram Configuration}
\usage{
setup_SpectrogramConfig(
  sample_rate = NULL,
  n_fft = 256L,
  window = "hanning",
  overlap = NULL,
  power = TRUE,
  db = TRUE,
  db_range = 80,
  freq_scale = "linear",
  freq_range = NULL,
  time_range = NULL,
  freq_unit = "Hz",
  time_unit = "s",
  colormap = "magma",
  colormap_reverse = FALSE,
  n_colors = 256L,
  zlim = NULL,
  show_colorbar = TRUE,
  colorbar_title = NULL,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL,
  dat_path = NULL,
  origin = NULL,
  writer = NULL
)
}
\arguments{
\item{sample_rate}{Optional Numeric \verb{[0, Inf)}: Signal sample rate in Hz.}

\item{n_fft}{Integer \verb{[2, Inf)}: FFT window length.}

\item{window}{Character: Window function.}

\item{overlap}{Optional Numeric \verb{[0, 1)}: Fractional window overlap.}

\item{power}{Logical: Use the power spectrum rather than magnitude.}

\item{db}{Logical: Convert to decibels.}

\item{db_range}{Numeric \verb{[0, Inf)}: Dynamic range in dB below the peak.}

\item{freq_scale}{Character \{"linear", "log", "mel"\}: Frequency axis scale.}

\item{freq_range, time_range}{Optional Numeric: Axis limits, length 2.}

\item{freq_unit, time_unit}{Character: Axis units, for the labels.}

\item{colormap}{Character: Continuous color scheme, or two or more hex
colors defining one.}

\item{colormap_reverse}{Logical: Reverse the color scheme.}

\item{n_colors}{Integer \verb{[2, Inf)}: Number of discrete colors.}

\item{zlim}{Optional Numeric: Color-scale limits, length 2.}

\item{show_colorbar}{Logical: Draw the color bar.}

\item{colorbar_title}{Optional Character: Color bar title.}

\item{xlab, ylab}{Optional Character: Axis labels.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Override the auto-computed margins, per side.}

\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{origin}{Optional Named character: Where each value came from. Normally
computed from which arguments were supplied; pass it only when restoring a
config that already carries provenance.}

\item{writer}{Optional Named character: Which interface wrote the config.}
}
\value{
\link{SpectrogramConfig} object.
}
\description{
The seam between convenient input and a complete, validated object. \strong{Every
argument is optional}, which is what lets the published schema require
nothing.
}
\examples{
m <- matrix(abs(rnorm(64 * 20)) + 1, nrow = 64)
draw(setup_SpectrogramConfig(), data = m)
}
\author{
EDG
}
