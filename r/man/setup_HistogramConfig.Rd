% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_histogram.R
\name{setup_HistogramConfig}
\alias{setup_HistogramConfig}
\title{Set up a Histogram Configuration}
\usage{
setup_HistogramConfig(
  x = NULL,
  group = NULL,
  breaks = "Sturges",
  palette = NULL,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  margin_top = NULL,
  margin_right = NULL,
  margin_bottom = NULL,
  margin_left = NULL,
  dat_path = NULL,
  origin = NULL,
  writer = NULL
)
}
\arguments{
\item{x}{Optional Character: Column to bin.}

\item{group}{Optional Character: Column to split the bins by.}

\item{breaks}{Character: Binning rule, passed to \code{\link[graphics:hist]{graphics::hist()}}. One of
the algorithm names it accepts, e.g. \code{"Sturges"}, \code{"Scott"}, \code{"FD"}.}

\item{palette}{Optional Character: Series colors, overriding the theme
palette for this chart. \code{NULL} uses the theme's.}

\item{xlab, ylab}{Optional Character: Axis labels. \code{NULL} derives them from
the data.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{margin_top, margin_right, margin_bottom, margin_left}{Optional Integer
\verb{[0, Inf)}: Plot margins in pixels.}

\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{origin}{Optional Named character: Where each value came from. Normally
computed from which arguments were supplied; pass it only when restoring a
config that already carries provenance.}

\item{writer}{Optional Named character: Which interface wrote the config.}
}
\value{
\link{HistogramConfig} object.
}
\description{
The seam between convenient input and a complete, validated object. \strong{Every
argument is optional}, which is what lets the published schema require
nothing.
}
\examples{
draw(setup_HistogramConfig(x = "mpg"), data = mtcars)
}
\author{
EDG
}
