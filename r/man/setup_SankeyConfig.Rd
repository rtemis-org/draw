% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_sankey.R
\name{setup_SankeyConfig}
\alias{setup_SankeyConfig}
\title{Set up a Sankey Diagram Configuration}
\usage{
setup_SankeyConfig(
  source = "source",
  target = "target",
  value = "value",
  orient = "horizontal",
  node_align = NULL,
  node_width = NULL,
  node_gap = NULL,
  palette = NULL,
  title = NULL,
  dat_path = NULL,
  origin = NULL,
  writer = NULL
)
}
\arguments{
\item{source}{Character: Column naming each link's source node.}

\item{target}{Character: Column naming each link's target node.}

\item{value}{Character: Column holding each link's magnitude.}

\item{orient}{Character \{"horizontal", "vertical"\}: Layout direction.}

\item{node_align}{Optional Character \{"left", "right", "justify"\}: How nodes
are aligned along the flow.}

\item{node_width}{Optional Numeric \verb{[0, Inf)}: Node thickness in pixels.}

\item{node_gap}{Optional Numeric \verb{[0, Inf)}: Gap between nodes in pixels.}

\item{palette}{Optional Character: Node colors, overriding the theme palette
for this chart. \code{NULL} uses the theme's.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{origin}{Optional Named character: Where each value came from. Normally
computed from which arguments were supplied; pass it only when restoring a
config that already carries provenance.}

\item{writer}{Optional Named character: Which interface wrote the config.}
}
\value{
\link{SankeyConfig} object.
}
\description{
The seam between convenient input and a complete, validated object. \strong{Every
argument is optional}, which is what lets the published schema require
nothing.
}
\examples{
links <- data.frame(
  source = c("a", "b"),
  target = c("c", "c"),
  value = c(1, 2)
)
draw(setup_SankeyConfig(), data = links)
}
\author{
EDG
}
