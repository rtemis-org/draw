% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{draw_heatmap}
\alias{draw_heatmap}
\title{Draw a Heatmap}
\usage{
draw_heatmap(
  x,
  row_names = NULL,
  col_names = NULL,
  triangle = NULL,
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  dist_method = "euclidean",
  hclust_method = "complete",
  square_cells = NULL,
  color = NULL,
  zlim = NULL,
  show_values = FALSE,
  value_digits = 2L,
  show_colorbar = TRUE,
  colorbar_orient = "vertical",
  title = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  filename = NULL
)
}
\arguments{
\item{x}{Numeric matrix: Input data. Rows map to y-axis categories and columns
to x-axis categories.}

\item{row_names}{Optional Character: Row labels. Defaults to \code{rownames(x)}, or
\code{"1"}, \code{"2"}, ... when row names are absent.}

\item{col_names}{Optional Character: Column labels. Defaults to \code{colnames(x)}.}

\item{triangle}{Optional Character \{"upper", "lower"\}: Mask one triangle of the
matrix to \code{NA}. \code{"upper"} keeps only the upper triangle; \code{"lower"} keeps
only the lower triangle. The diagonal is always masked, as it is
uninformative for symmetric matrices (e.g. always 1 for correlations).}

\item{cluster_rows}{Logical: Whether to reorder rows via hierarchical clustering.}

\item{cluster_cols}{Logical: Whether to reorder columns via hierarchical clustering.}

\item{dist_method}{Character: Distance method passed to \code{\link[stats:dist]{stats::dist()}}.
Common values: \code{"euclidean"}, \code{"manhattan"}.}

\item{hclust_method}{Character: Linkage method passed to \code{\link[stats:hclust]{stats::hclust()}}.
Common values: \code{"complete"}, \code{"ward.D2"}, \code{"average"}.}

\item{square_cells}{Optional Logical: Whether to compute widget dimensions so
cells are square. \code{NULL} (default) enables this automatically for square
matrices (e.g. correlation matrices). When \code{TRUE}, both \code{width} and \code{height}
are calculated from the number of cells; supply explicit \code{width}/\code{height} to
override.}

\item{color}{Optional Character: Color palette — a vector of 2 or more colors
defining the continuous color scale from \code{zlim[1]} to \code{zlim[2]}. When \code{NULL}
(default) a diverging teal–white–orange palette is used when data spans zero,
otherwise a sequential single-hue palette is used.}

\item{zlim}{Optional Numeric: Length-2 vector \code{c(min, max)} for the color scale.
Defaults to the observed data range. For correlation matrices, \code{c(-1, 1)} is
recommended.}

\item{show_values}{Logical: Whether to print the cell value as a label inside
each cell.}

\item{value_digits}{Integer: Decimal places used in cell labels and the tooltip.}

\item{show_colorbar}{Logical: Whether to display the continuous color-scale bar.}

\item{colorbar_orient}{Character \{"vertical", "horizontal"\}: Orientation of the
color bar.}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override. \code{NULL} auto-detects light/dark mode.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}
}
\value{
htmlwidget: Widget object.
}
\description{
Quick heatmap from a numeric matrix. Designed to handle all common use cases
including correlation matrices (square cells, diverging color scale),
general rectangular heatmaps, and clustered heatmaps.
}
\details{
Color encoding is driven by a continuous \link{VisualMap} component.
Hierarchical clustering is performed in R via \code{\link[=hclust]{hclust()}}; rows and columns
are reordered accordingly (dendrogram rendering is not yet supported).
}
