% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_confusion.R
\name{ConfusionConfig}
\alias{ConfusionConfig}
\title{Confusion Plot Configuration}
\usage{
ConfusionConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  reference = "reference",
  predicted = "predicted",
  count = "n",
  panel = NULL,
  classes = NULL,
  show_metrics = TRUE,
  digits = 2L,
  ncol = 2L,
  correct_color = "#0F6A66",
  incorrect_color = "#BE2E5F",
  low_color = NULL,
  summary_color = NULL,
  font_size = 12,
  xlab = "Predicted",
  ylab = "Reference"
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{reference}{Character: Column containing reference class labels.}

\item{predicted}{Character: Column containing predicted class labels.}

\item{count}{Character: Column containing nonnegative integer frequencies.}

\item{panel}{Optional Character: Optional panel-label column; unset draws one matrix.}

\item{classes}{Optional Character vector: Ordered class labels shared by every panel; unset preserves first appearance.}

\item{show_metrics}{Logical: Show per-class rates, accuracy, and balanced accuracy.}

\item{digits}{Integer: Decimal places for rates in cells and hover text.}

\item{ncol}{Integer: Maximum number of panels per row.}

\item{correct_color}{Character: Six-digit hex color at unit fraction for correct predictions.}

\item{incorrect_color}{Character: Six-digit hex color at unit fraction for incorrect predictions.}

\item{low_color}{Optional Character: Six-digit hex color at zero row fraction;
unset uses the active theme background.}

\item{summary_color}{Optional Character: Six-digit hex background for metric
cells; unset uses a faint neutral tint of the active theme background.}

\item{font_size}{Numeric: Cell-label font size in pixels.}

\item{xlab}{Character: Predicted-class axis label.}

\item{ylab}{Character: Reference-class axis label.}
}
\value{
A \code{ConfusionConfig} object.
}
\description{
Portable confusion counts with reference rows and predicted columns.
Data consists of long records with class labels and a frequency, optionally
grouped into panels. Missing class labels represent omitted pairs; absent
class combinations have zero frequency. All panels use the same class order.
Compiles to \code{HeatmapSeriesOption} in \code{src/chart/heatmap/HeatmapSeries.ts},
\code{GridOption} in \code{src/coord/cartesian/GridModel.ts}, and continuous visual maps.
\code{to_list()} emits semantic config keys; \code{compile()} emits ECharts options.
\code{draw()} fits square count cells with aligned marginal summaries to the
available canvas. Missing-pair omissions appear below the matrix when present.
The cell counts show included observations without a separate sample-size label. Color fades and
marginal backgrounds follow the active theme unless explicitly overridden.
}
\section{Statistical semantics}{

Color intensity is the fraction within each reference row; labels show raw
counts. Sensitivity, specificity, PPV, and NPV are one-versus-rest rates for
each named class. Balanced accuracy is mean class recall, including every
declared class. Zero denominators produce NA, never zero; consequently
balanced accuracy is NA if any declared class has no reference observations.
Summaries of pooled resamples describe pooled predictions, not mean fold
performance. Repeated observations are counted each time they appear.
}

\examples{
cfg <- setup_ConfusionConfig()
draw(cfg, data = data.frame(reference = c("yes", "no"),
  predicted = c("yes", "no"), n = c(8, 12)))
}
