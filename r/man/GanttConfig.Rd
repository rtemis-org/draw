% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_gantt.R
\name{GanttConfig}
\alias{GanttConfig}
\title{Gantt Chart Configuration}
\usage{
GanttConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  label = "label",
  start = "start",
  end = "end",
  group = NULL,
  tooltip = NULL,
  border = NULL,
  axis_type = "value",
  zoom = TRUE,
  guides = TRUE,
  bar_height = 0.6,
  bar_radius = 0,
  border_color = "#E53935",
  border_width = 1.5,
  palette = NULL,
  xlab = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{label}{Character: Column naming each task.}

\item{start}{Character: Column holding each task's start.}

\item{end}{Character: Column holding each task's end.}

\item{group}{Optional Character: Column to color bars by.}

\item{tooltip}{Optional Character: Column holding per-bar tooltip text.}

\item{border}{Optional Character: Logical column; \code{TRUE} outlines the bar.}

\item{axis_type}{Character \{"value", "time"\}: How the time axis is scaled.}

\item{zoom}{Logical: Enable the zoom control.}

\item{guides}{Logical: Show guide lines.}

\item{bar_height}{Numeric \verb{[0, 1]}: Bar height as a fraction of the row.}

\item{bar_radius}{Numeric \verb{[0, Inf)}: Bar corner radius in pixels.}

\item{border_color}{Character: Outline color for bordered bars.}

\item{border_width}{Numeric \verb{[0, Inf)}: Outline width in pixels.}

\item{palette}{Optional Character: Bar colors, overriding the theme palette
for this chart. \code{NULL} uses the theme's.}

\item{xlab}{Optional Character: Time axis label.}
}
\value{
\code{GanttConfig} object.
}
\description{
A serializable description of a Gantt chart. Build one with
\code{\link[=setup_GanttConfig]{setup_GanttConfig()}} rather than calling this constructor directly.
}
\details{
The bound data is a table of tasks, one row per bar. \code{label}, \code{start} and
\code{end} name its required columns; \code{group}, \code{tooltip} and \code{border} name
optional ones.
}
\examples{
setup_GanttConfig()@type
}
\author{
EDG
}
