% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_fit.R
\name{draw_fit}
\alias{draw_fit}
\title{Draw True Versus Predicted Values}
\usage{
draw_fit(
  x,
  y,
  group = NULL,
  fit = "glm",
  se = TRUE,
  se_times = 1.96,
  rsq = TRUE,
  diagonal = TRUE,
  diagonal_color = NULL,
  square = TRUE,
  equal_axes = TRUE,
  xlab = "True",
  ylab = "Predicted",
  ...
)
}
\arguments{
\item{x}{Numeric vector, single-column matrix, or list: True values.}

\item{y}{Numeric vector, single-column matrix, or list: Predicted values.}

\item{group}{Optional Atomic vector or single-column data frame: Grouping
variable for multiple series.}

\item{fit}{Optional Character \{"glm", "gam"\}: Fit method. \code{NULL} disables fitting.
\code{"gam"} for \code{\link[mgcv:gam]{mgcv::gam()}}. The fitted line and standard-error band
are computed per group when \code{group} is provided.}

\item{se}{Logical: Whether to show the confidence band.}

\item{se_times}{Numeric \verb{[0, Inf)}: Standard-error multiplier for the band.}

\item{rsq}{Logical: Include the fitted model's R-squared in series labels.
This describes the overlay fit, not predictive performance against the
identity line. Constant responses have undefined R-squared, labeled \code{NA}.}

\item{diagonal}{Logical: Draw a dashed identity line within the axis limits.}

\item{diagonal_color}{Optional Character: Identity-line color.}

\item{square}{Logical: If TRUE, draw the plotting box square -- equal height
and width in pixels, excluding axis labels and margins.}

\item{equal_axes}{Logical: If TRUE, give one data unit the same size in
pixels on both axes. Set both for a plot that is square \emph{and} to scale,
such as a true-versus-predicted plot whose identity line should run at 45
degrees; the two axes are then made to span the same interval. See
Details.}

\item{xlab}{Optional Character: X-axis title.}

\item{ylab}{Optional Character: Y-axis title.}

\item{...}{Additional arguments to \code{\link[=draw_scatter]{draw_scatter()}}, including \code{theme},
\code{palette}, limits, margins, dimensions, and \code{filename}.}
}
\value{
htmlwidget: ECharts drawing.
}
\description{
Draw paired observations with a fitted line, standard-error band, and an
identity line. The plotting box is square and both axes share an interval.
Named lists support samples of different lengths, with one fit per sample.
}
\details{
The overlay uses the same implementation as \code{\link[=draw_scatter]{draw_scatter()}} and
\link{ScatterConfig}. The fit is GLM or GAM; \code{fit = NULL} disables it. Missing
pairs and observations with missing groups are dropped together. Infinite
values, unequal pair lengths, and multiple response columns are rejected.
List names are matched before drawing; unnamed lists are paired by position.
Paired \code{NULL} entries in lists represent unavailable sets and are omitted.

The equivalent portable configuration is a \link{ScatterConfig} binding true,
predicted, and sample columns, with \code{fit}, \code{se}, \code{rsq}, \code{diagonal}, \code{square},
and \code{equal_axes} set explicitly. Model objects are not needed to render it.
}
\examples{
draw_fit(1:5, c(1.2, 1.8, 3.3, 3.8, 5.2))
draw_fit(
  list(Training = 1:5, Test = 2:5),
  list(Training = c(1.2, 1.8, 3.3, 3.8, 5.2), Test = c(2.2, 2.7, 4.3, 4.8))
)
}
