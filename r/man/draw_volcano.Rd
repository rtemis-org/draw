% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_significance.R
\name{draw_volcano}
\alias{draw_volcano}
\title{Draw a Volcano Plot}
\usage{
draw_volcano(
  x,
  pvals,
  xnames = NULL,
  p_adjust_method = "holm",
  p_transform = "neg_log10",
  p_thresh = 0.05,
  x_thresh = 0,
  annotate_n = 7L,
  ...,
  legend_position = "top",
  legend_placement = "outside",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL,
  filename = NULL
)
}
\arguments{
\item{x}{Numeric vector: Signed estimates; NA values have no plotted mark.}

\item{pvals}{Numeric vector: Raw p-values in \verb{[0, 1]}, or NA.}

\item{xnames}{Optional Character vector: One outcome label per estimate.}

\item{p_adjust_method}{Character: Method from \link[stats:p.adjust.methods]{stats::p.adjust.methods}. Adjustment precedes transformation.}

\item{p_transform}{Character \{"neg_log10", "identity", "one_minus"\}: Named display transform of adjusted p-values.}

\item{p_thresh}{Numeric (0, 1]: Significance threshold on adjusted p-values; the comparison is strict.}

\item{x_thresh}{Numeric: Effect threshold separating low and high significant results.}

\item{annotate_n}{Integer [0, Inf): Maximum significant feature labels per side in the volcano view, ranked by raw p-value with input-order ties.}

\item{...}{Additional named settings for \code{\link[=setup_SignificanceConfig]{setup_SignificanceConfig()}}, such as
\code{reference}, \code{zero_cap}, \code{n_tests}, colors, axis limits, or margins.}

\item{legend_position}{Character \{"top", "bottom", "left", "right",
"top-left", "top-right", "bottom-left", "bottom-right"\}: Legend anchor.
Top/bottom anchors use horizontal rows; left/right anchors use a vertical
column. Corner anchors align within the top or bottom row.}

\item{legend_placement}{Character \{"outside", "inside"\}: Relation to the
plotting area. Outside placement reserves space for the complete legend;
inside placement overlays the data. Neither setting adds a missing legend.}

\item{theme}{Optional \link{Theme}: Widget/export theme.}

\item{width, height}{Optional Numeric or Character: Widget dimensions.}

\item{element_id}{Optional Character: HTML element identifier.}

\item{filename}{Optional Character: Static output path, currently SVG.}
}
\value{
An ECharts htmlwidget.
}
\description{
Plot signed estimates and raw p-values through \link{SignificanceConfig}.
The Manhattan view places outcomes in input order; it does not use genomic
coordinates. Missing pairs have no marks but remain in the testing family.
}
\section{Statistical semantics}{

P-values are adjusted once over the full supplied family before rows with
missing estimates or p-values are omitted from the marks. Missing hypotheses
still count in \code{n_tests}; they do not become observed p-values of one.
Raw and adjusted values remain in the compiled data. Function-valued
transforms are not accepted by this portable configuration.

Under \code{neg_log10}, zero p-values have infinite mathematical height. They
retain their zero value but are displayed at \code{zero_cap}, identified by
triangles and a caption. No positive p-value is clamped. For identity and
one-minus transforms, zeros are finite and no cap is used.
}

\examples{
draw_volcano(c(-2, 0.1, 3), c(0.001, 0.6, 0.002),
  xnames = c("A", "B", "C"), p_adjust_method = "holm")
}
