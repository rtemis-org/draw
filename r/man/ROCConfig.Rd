% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_roc.R
\name{ROCConfig}
\alias{ROCConfig}
\title{ROC Plot Configuration}
\usage{
ROCConfig(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  fpr = "fpr",
  tpr = "tpr",
  auc = NULL,
  class_label = NULL,
  split = NULL,
  fold = NULL,
  omitted = NULL,
  variant = "aggregate",
  digits = 3L,
  diagonal = TRUE,
  diagonal_color = "#888888",
  palette = NULL,
  legend = TRUE,
  legend_position = "bottom-right",
  square = TRUE,
  line_width = 2,
  fold_opacity = 0.45,
  xlab = "False positive rate",
  ylab = "True positive rate"
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{fpr}{Character: Column containing false positive rates.}

\item{tpr}{Character: Column containing true positive rates.}

\item{auc}{Optional Character: Optional supplied AUC column; unset integrates the supplied vertices.}

\item{class_label}{Optional Character: Optional positive-class label column.}

\item{split}{Optional Character: Optional sample or model label column.}

\item{fold}{Optional Character: Optional resample column; aggregate identifies pooled curves.}

\item{omitted}{Optional Character: Optional omitted-observation count column, constant within each curve.}

\item{variant}{Character: Draw pooled curves or separate resample curves.}

\item{digits}{Integer: Decimal places for AUC labels and tooltip values.}

\item{diagonal}{Logical: Show an independent chance diagonal.}

\item{diagonal_color}{Character: Chance-line color.}

\item{palette}{Optional Character: Group colors; unset uses the chart theme.}

\item{legend}{Logical: Show group labels and AUC summaries.}

\item{legend_position}{Character \{"bottom-right", "top-right", "top-left", "bottom-left"\}: Corner inside the plotting area for the legend.}

\item{square}{Logical: Keep the plotting grid square.}

\item{line_width}{Numeric: Curve stroke width in pixels.}

\item{fold_opacity}{Numeric: Opacity of individual resample curves.}

\item{xlab}{Character: Horizontal axis label.}

\item{ylab}{Character: Vertical axis label.}
}
\value{
A \code{ROCConfig} object.
}
\description{
Portable bindings for long ROC records. Each split/class/fold identifies one
curve. The reserved fold label \code{aggregate} identifies pooled predictions.
Compiles to \code{LineSeriesOption} in \code{src/chart/line/LineSeries.ts},
\code{GridOption} in \code{src/coord/cartesian/GridModel.ts}, and \code{LegendOption} in
\code{src/component/legend/LegendModel.ts}.
}
\section{Statistical semantics}{

Vertices are ordered by FPR then TPR and joined without smoothing. Curves
must be monotone and include (0, 0) and (1, 1). Supplied AUC is authoritative
and may describe a higher-resolution curve. Without an AUC binding, the
supplied vertices define trapezoidal AUC. An undefined curve is represented
by missing FPR, TPR, and AUC and is reported in the console when omitted.
Per-resample legends report the unweighted mean and sample SD of defined
fold AUCs, with available/total curve counts. SD is NA for one defined fold.
Pooled AUC is never inferred from per-fold AUCs.
The legend is inset in the chosen corner of the plotting area. Curve identity
and AUC share a line; long labels wrap to the available width. All entries
remain present in static SVG output, without a scrolling legend.
Hover describes the selected curve, including its resample identifier.
Numeric tooltip labels use \code{digits}; coordinates and AUCs retain their full
precision in the compiled data.
}

\examples{
draw(setup_ROCConfig(), data = data.frame(fpr = c(0, 0, 1), tpr = c(0, 1, 1)))
}
