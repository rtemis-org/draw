% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_varimp.R
\name{draw_varimp}
\alias{draw_varimp}
\title{Draw Variable Importance}
\usage{
draw_varimp(
  x,
  measure = NULL,
  top_n = 20L,
  rank_by = "magnitude",
  summary = "mean",
  absent = "missing",
  folds = NULL,
  horizontal = TRUE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL,
  filename = NULL,
  type = "bar",
  decreasing = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector, single-column matrix, or data frame: Importance
scores. Tables require \code{variable} and named numeric measures, optionally
\code{fold}. Variable names must be unique within each fold.}

\item{measure}{Optional Character: Measure column. NULL selects the first
column other than \code{variable} or \code{fold}; vectors use \code{"importance"}.}

\item{top_n}{Optional Integer \verb{[1, Inf)}: Maximum number of variables.
NULL includes all variables with a summary; fractions are not accepted.}

\item{rank_by}{Character \{"magnitude", "signed"\}: Rank by absolute or signed
summary, respectively.}

\item{summary}{Character \{"mean", "median"\}: Summary across folds.}

\item{absent}{Character \{"missing", "zero"\}: Meaning of omitted variable
rows within a fold reporting the selected measure.}

\item{folds}{Optional Character: Full set of fold IDs, including folds with
no rows. NULL uses the IDs observed in the \code{fold} column.}

\item{horizontal}{Logical: Draw horizontal bars or boxes.}

\item{xlab, ylab}{Optional Character: Physical axis labels. NULL derives the
score label from the measure and summary and labels the variable axis.}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override. Set \code{palette} in \code{...} to
override its series colors.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height. NULL allocates
space per selected variable for horizontal bars.}

\item{element_id}{Optional Character: Explicit DOM element ID for the widget
container. \code{NULL} lets htmlwidgets generate one.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}

\item{type}{Character \{"bar", "boxplot"\}: Summary or fold-distribution view.}

\item{decreasing}{Logical: Select and display ranks from largest to smallest.
FALSE selects and displays the smallest ranks first.}

\item{...}{Additional settings for \code{\link[=setup_BarConfig]{setup_BarConfig()}} or
\code{\link[=setup_BoxplotConfig]{setup_BoxplotConfig()}}, according to \code{type}. Boxplots accept \code{boxpoints},
\code{quartiles}, \code{whisker}, and point styling; unset \code{boxpoints} shows all scores.
Data bindings, labels, and orientation are set here.}
}
\value{
htmlwidget: ECharts importance bars or fold distributions.
}
\description{
Draw signed importance summaries with \link{BarConfig}, or fold distributions
with \link{BoxplotConfig} using \code{type = "boxplot"}. Numeric input
uses its names as variable labels, or row numbers when unnamed. Tables
preserve named measures and optionally identify resamples with \code{fold}.
}
\details{
Resampled scores are summarized before selection. Magnitude ranking uses
the absolute summary, not the mean absolute fold score. Signed ranking uses
the summary itself. \code{decreasing = FALSE} selects the smallest ranks first.
Ties retain first appearance in the input. The first selected variable
appears at the top of horizontal bars or the left of
vertical bars or boxes. Zero scores are retained. For distributions, the
summary controls selection and ordering only; boxes use the fold values.
Ranking direction is never inferred from a measure name. For smaller-is-better
scores, use \verb{rank_by = "signed", decreasing = FALSE}. Averaging fold p-values
does not produce a combined p-value; choose a suitable summary outside this
function when the score's interpretation requires it.

Set \code{bar_width} to a pixel thickness for separate zero-to-score segments.
This uses native bars and covers the legacy importance \code{type = "line"}
geometry without connecting different variables or adding a new chart type.

Explicit NA scores and wholly unavailable folds are excluded from each
variable's summary. Variables with no available score are omitted.
\code{absent = "zero"} declares that omitted variable rows mean known zero scores
within folds reporting that measure. It never replaces an explicit NA or a
wholly unavailable fold/measure with zero. Use it only for sparse importance
tables whose producer omits zero entries. Infinite scores are rejected.

Means and medians give each contributing fold equal weight. An incomplete
summary describes the available folds, not an estimate guaranteed to be
unbiased for all folds. Incomplete fold coverage is reported in the console;
chart labels retain only the variable and measure names. The input is never
modified.

Boxplots require fold-level records. Every available score (including known
structural zeros) is overlaid by default, with its fold ID in the tooltip.
Missing values remain missing in the materialized table and are reported in
the console. Boxes describe resample variability, not a confidence interval.
See \code{\link[=draw_boxplot]{draw_boxplot()}} for quartiles, whiskers, and point placement. Use
\code{whisker = 0} for full-range whiskers, as in the current live importance view.

For portable rendering, materialize the selected, summarized \code{label}
and \code{importance} columns in display order and bind them using
\code{setup_BarConfig(x = "label", y = "importance")}. Preserve variable IDs and
contributing/total fold counts alongside these columns. Set \code{horizontal},
axis labels, and title explicitly. That table and config reproduce the view
without a fitted model or R callbacks. The raw-data summary and selection
options are not yet part of a shared visualization schema. For distributions,
use one numeric column per selected variable and an observation column for
the complete fold universe. Bind those columns and the variable
labels through \code{\link[=setup_BoxplotConfig]{setup_BoxplotConfig()}}. Only selected variables are widened;
selection does not discard folds needed to identify structural zeros.
}
\examples{
draw_varimp(c(age = 0.8, weight = -0.4, height = 0.2))
scores <- data.frame(
  variable = c("age", "weight", "age"),
  fold = c("A", "A", "B"),
  gain = c(0.8, 0.4, 0.6)
)
draw_varimp(scores, measure = "gain", absent = "zero")
draw_varimp(scores, measure = "gain", type = "boxplot", absent = "zero")
}
