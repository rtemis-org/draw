% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_a3.R
\name{A3Config}
\alias{A3Config}
\title{Annotated Protein Diagram Configuration}
\usage{
A3Config(
  dat_path = NULL,
  title = NULL,
  origin = NULL,
  writer = NULL,
  n_per_row = 21L,
  position_every = 10L,
  ptm_placement = "radial",
  zoom = TRUE,
  residue_spacing = 0.3,
  marker_size = 28,
  font_size = 18,
  line_width = 2,
  show_markers = TRUE,
  show_labels = TRUE,
  region_opacity = 0.3,
  residue_fill = NULL,
  residue_stroke = NULL,
  label_color = NULL,
  pos_label_color = NULL,
  variant_color = NULL,
  disease_variant_color = NULL
)
}
\arguments{
\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{origin}{Optional Named character \{"user", "default", "derived"\}: Where
each value came from, one entry per settable property. Absent on an
authored config; written by the interface that resolved it.}

\item{writer}{Optional Named character: Which interface wrote the config, as
\code{name} and \code{version}. Absent on an authored config.}

\item{n_per_row}{Integer \verb{[1, Inf)}: Residues per row.}

\item{position_every}{Integer \verb{[1, Inf)}: Label every nth position.}

\item{ptm_placement}{Character: How post-translational modifications are
placed relative to the backbone.}

\item{zoom}{Logical: Enable the zoom control.}

\item{residue_spacing}{Numeric \verb{[0, Inf)}: Gap between residues.}

\item{marker_size}{Numeric \verb{[0, Inf)}: Preferred residue marker size;
reduced with related marks when needed to fit the surface.}

\item{font_size}{Numeric \verb{[0, Inf)}: Residue label font size.}

\item{line_width}{Numeric \verb{[0, Inf)}: Backbone line width.}

\item{show_markers, show_labels}{Logical: Whether to draw each.}

\item{region_opacity}{Numeric \verb{[0, 1]}: Region band opacity.}

\item{residue_fill, residue_stroke}{Optional Character: Residue marker colors.}

\item{label_color, pos_label_color}{Optional Character: Label colors.}

\item{variant_color, disease_variant_color}{Optional Character: Variant marker colors.
\code{NULL} selects the corresponding light/dark A3 theme color.}
}
\value{
\code{A3Config} object.
}
\description{
A serializable description of an A3 protein diagram. Build one with
\code{\link[=setup_A3Config]{setup_A3Config()}} rather than calling this constructor directly.
}
\details{
The bound data is an \code{A3} object, created with \code{rtemis.a3::create_A3()}.
}
\examples{
setup_A3Config(n_per_row = 30L)@type
}
\author{
EDG
}
