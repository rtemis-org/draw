% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{draw_boxplot}
\alias{draw_boxplot}
\title{Draw a Boxplot}
\usage{
draw_boxplot(
  x,
  labels = NULL,
  group = NULL,
  horizontal = FALSE,
  palette = NULL,
  fill_alpha = 0.25,
  na_rm = TRUE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  theme = NULL,
  margins = DEFAULT_MARGINS,
  width = NULL,
  height = NULL,
  verbosity = 1L,
  element_id = NULL,
  filename = NULL
)
}
\arguments{
\item{x}{Numeric or list: A list of numeric vectors (one per box), or a single numeric
vector when \code{group} is provided. For ungrouped named lists, \code{names(data)}
are used as labels when \code{labels} is not supplied.}

\item{labels}{Optional Character: Category labels for each box. Ignored when \code{group} is
provided (group levels are used instead). When omitted for ungrouped named
lists, \code{names(data)} are used.}

\item{group}{Optional Vector: Grouping variable. When provided, \code{data} must be a
numeric vector, and boxplot statistics are computed per group. Each group
gets its own colored series.}

\item{horizontal}{Logical: Whether to draw horizontal boxplots.}

\item{palette}{Optional Character: Box color or colors. For ungrouped boxplots, a single color used at
full opacity for borders and at \code{fill_alpha} opacity for the fill.
For grouped boxplots, defaults to \code{rtemis_colors}; recycled as needed.}

\item{fill_alpha}{Numeric \verb{[0, 1]}: Opacity for the box fill color.}

\item{na_rm}{Logical: Whether to remove \code{NA} values before
computing boxplot statistics.}

\item{xlab}{Optional Character: X-axis title (bottom axis). Overrides the
value-axis name inferred from a single-element named list passed as \code{data}.}

\item{ylab}{Optional Character: Y-axis title (left axis). Overrides the
value-axis name inferred from a single-element named list passed as \code{data}.}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override.}

\item{margins}{Optional Named numeric vector or named list: Plot margins in
pixels (or percentage strings) for any of \code{"top"}, \code{"right"},
\code{"bottom"}, \code{"left"}. Unspecified sides keep echarts' default auto-sizing.
See \code{\link[=draw_line]{draw_line()}} for details.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{verbosity}{Integer \verb{[0, Inf)}: Verbosity level for removed-\code{NA} messages.}

\item{element_id}{Optional Character: Explicit DOM element ID for the widget
container. \code{NULL} lets htmlwidgets generate one.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}
}
\value{
htmlwidget: Widget object.
}
\description{
Quick boxplot from raw data with optional grouping for multiple traces.
Boxplot statistics (min, Q1, median, Q3, max) are computed automatically
using \code{\link[grDevices:boxplot.stats]{grDevices::boxplot.stats()}}.
}
