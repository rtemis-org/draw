% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_boxplot.R
\name{draw_boxplot}
\alias{draw_boxplot}
\title{Draw a Boxplot with Observation Overlays}
\usage{
draw_boxplot(
  x,
  labels = NULL,
  group = NULL,
  horizontal = FALSE,
  palette = NULL,
  fill_alpha = 0.25,
  na_rm = TRUE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  theme = NULL,
  margins = DEFAULT_MARGINS,
  width = NULL,
  height = NULL,
  verbosity = 1L,
  element_id = NULL,
  filename = NULL,
  observation = NULL,
  quartiles = "linear",
  whisker = 1.5,
  boxpoints = "none",
  point_size = 5,
  point_alpha = 0.6,
  point_spread = 0.5,
  legend_position = "top",
  legend_placement = "outside"
)
}
\arguments{
\item{x}{Numeric or List: Numeric vectors, one per box; named lists supply
category labels. With \code{group}, every vector must match its length.}

\item{labels}{Optional Character: One category label per vector. A single
grouped vector uses group labels instead.}

\item{group}{Optional Atomic vector or single-column data frame: Group
identities in first-appearance order. The column name does not add a legend
or heading; both input forms produce the same plot.}

\item{horizontal}{Logical: Draw horizontal boxes.}

\item{palette}{Optional Character: Box colors, recycled across boxes or groups.}

\item{fill_alpha}{Numeric \verb{[0, 1]}: Box fill opacity.}

\item{na_rm}{Logical: Remove missing values. FALSE rejects missing input.}

\item{xlab, ylab}{Optional Character: Axis labels.}

\item{title}{Optional Character: Chart title.}

\item{theme}{Optional \link{Theme}: Theme override. The palette inside the theme can be
overridden per-chart with the \code{color} argument.}

\item{margins}{Optional Named numeric vector or named list: Plot margins in
pixels (or percentage strings) for any of \code{"top"}, \code{"right"},
\code{"bottom"}, \code{"left"} — e.g. \code{c(left = 80, right = 20)} or
\code{list(left = 80, right = "10\%")}. Unspecified sides keep echarts' default
auto-sizing (\code{outerBoundsMode = "same"}), which shrinks the grid to fit
axis labels and names with no dead space.}

\item{width}{Optional Character or Numeric: Widget width.}

\item{height}{Optional Character or Numeric: Widget height.}

\item{verbosity}{Integer \verb{[0, Inf)}: Verbosity for missing-value messages.}

\item{element_id}{Optional Character: Explicit DOM element ID for the widget
container. \code{NULL} lets htmlwidgets generate one.}

\item{filename}{Optional Character: If provided, save the widget to this file via
\code{\link[=save_drawing]{save_drawing()}}.}

\item{observation}{Optional Atomic vector: Observation identifiers in tooltips,
aligned to every input vector. Unset uses original row numbers.}

\item{quartiles}{Character \{"linear", "hinges"\}: Quartile convention.}

\item{whisker}{Numeric \verb{[0, Inf)}: Finite IQR fence multiplier;
zero uses full-range whiskers.}

\item{boxpoints}{Character \{"none", "all", "outliers"\}: Values to overlay.}

\item{point_size}{Numeric \verb{(0, Inf)}: Finite point diameter in pixels.}

\item{point_alpha}{Numeric \verb{[0, 1]}: Point opacity.}

\item{point_spread}{Numeric \verb{[0, 1]}: Fraction of box width occupied by offsets.}

\item{legend_position}{Character \{"top", "bottom", "left", "right",
"top-left", "top-right", "bottom-left", "bottom-right"\}: Legend anchor.
Top/bottom anchors use horizontal rows; left/right anchors use a vertical
column. Corner anchors align within the top or bottom row.}

\item{legend_placement}{Character \{"outside", "inside"\}: Relation to the
plotting area. Outside placement reserves space for the complete legend;
inside placement overlays the data. Neither setting adds a missing legend.}
}
\value{
htmlwidget: ECharts boxes with optional vector point overlays.
}
\description{
Draw one box per numeric vector, optionally split by groups. Statistics use
linear-interpolation quartiles (\code{stats::quantile(type = 7)}) or Tukey hinges
(\code{stats::fivenum()}). Whiskers extend to observations within the quartiles
plus/minus \code{whisker} times their difference, including the quartiles
themselves. \code{whisker = 0} uses the full observed range. Values strictly
outside the fences are outliers; they are never removed from the quartiles.
}
\details{
All-NA and empty boxes retain their category without a mark; entirely
unavailable input is an error. Missing values are reported in the console
according to \code{verbosity} and never replaced with zero. Missing group
assignments are excluded. Axis titles are centered. The category-axis
baseline is shown only when the plotted value range includes zero.
Every category label is shown so boxes remain identifiable in narrow plots.
Use a horizontal layout or a larger figure for many or long category names.
With point overlays, padded range endpoints are unlabeled; interior ticks
retain the backend's numeric formatting and plotted values are unchanged.
Points retain their exact value coordinate. Their perpendicular offsets use
a deterministic base-two sequence in input row order within \code{point_spread}
times the box width. Zero spread centers every point; coincident points can
overlap. No random state is read or changed. Browser and SVG share the same
named point renderer, including grouped-box offsets and legend filtering.
}
\examples{
draw_boxplot(list(Training = c(.8, .9, .85), Test = c(.7, .8, .75)),
  boxpoints = "all", observation = c("Fold1", "Fold2", "Fold3"))
draw_boxplot(iris["Sepal.Length"], group = iris["Species"])
}
