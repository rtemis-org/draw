% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_massglm.R
\name{plot_manhattan}
\alias{plot_manhattan}
\alias{plot.MassGLM}
\title{Plot a MassGLM Result}
\usage{
plot_manhattan(x, coefname = NULL, ...)
}
\arguments{
\item{x}{\code{rtemis::MassGLM}: Fitted mass-univariate model.}

\item{coefname}{Optional Character scalar: Coefficient to plot. Unset selects
the first entry in the object's \code{coefnames}.}

\item{...}{Named settings for \code{\link[=draw_volcano]{draw_volcano()}} or \code{\link[=draw_manhattan]{draw_manhattan()}}.}
}
\value{
An ECharts htmlwidget.
}
\description{
\code{plot(model)} draws a volcano plot when rtemis.draw is loaded.
\code{rtemis.draw::plot_manhattan(model)} draws categorical significance bars,
preserving outcome order. Both select one coefficient and use the shared
\link{SignificanceConfig} statistical contract via \code{\link[=draw_volcano]{draw_volcano()}} or
\code{\link[=draw_manhattan]{draw_manhattan()}}.
}
\details{
Draw registers its method on the existing \code{\link[graphics:plot]{graphics::plot()}} generic for
the S7 MassGLM class, replacing that class's legacy method while draw is
loaded. The prior method is restored on namespace unload if draw still owns
the registration. This does not replace rtemis's \code{plot_manhattan} generic;
qualify the draw-owned generic when both packages are attached.
}
\examples{
\dontshow{if (requireNamespace("rtemis", quietly = TRUE)) withAutoprint(\{ # examplesIf}
model <- rtemis::massGLM(mtcars["wt"], mtcars[c("mpg", "hp", "disp")],
                         verbosity = 0L)
plot(model, coefname = "wt")
rtemis.draw::plot_manhattan(model, coefname = "wt")
\dontshow{\}) # examplesIf}
}
