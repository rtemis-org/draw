% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_a3.R
\name{setup_A3Config}
\alias{setup_A3Config}
\title{Set up an Annotated Protein Diagram Configuration}
\usage{
setup_A3Config(
  n_per_row = 21L,
  position_every = 10L,
  ptm_placement = "radial",
  zoom = TRUE,
  residue_spacing = 0.3,
  marker_size = 28,
  font_size = 18,
  line_width = 2,
  show_markers = TRUE,
  show_labels = TRUE,
  region_opacity = 0.35,
  residue_fill = "#E7E5E4",
  residue_stroke = "#44403C",
  label_color = "#1C1917",
  pos_label_color = "#78716C",
  variant_color = "#FA6E1E",
  disease_variant_color = "#E266AE",
  title = NULL,
  dat_path = NULL,
  origin = NULL,
  writer = NULL
)
}
\arguments{
\item{n_per_row}{Integer \verb{[1, Inf)}: Residues per row.}

\item{position_every}{Integer \verb{[1, Inf)}: Label every nth position.}

\item{ptm_placement}{Character: How post-translational modifications are
placed relative to the backbone.}

\item{zoom}{Logical: Enable the zoom control.}

\item{residue_spacing}{Numeric \verb{[0, Inf)}: Gap between residues.}

\item{marker_size}{Numeric \verb{[0, Inf)}: Residue marker size.}

\item{font_size}{Numeric \verb{[0, Inf)}: Residue label font size.}

\item{line_width}{Numeric \verb{[0, Inf)}: Backbone line width.}

\item{show_markers, show_labels}{Logical: Whether to draw each.}

\item{region_opacity}{Numeric \verb{[0, 1]}: Region band opacity.}

\item{residue_fill, residue_stroke}{Character: Residue marker colors.}

\item{label_color, pos_label_color}{Character: Label colors.}

\item{variant_color, disease_variant_color}{Character: Variant marker colors.}

\item{title}{Optional Character: Chart title. Declared here because it is the
one property every chart type has; \code{palette} is not (four take a \code{colormap}
instead), and axis labels and margins are cartesian-only.}

\item{dat_path}{Optional Character: Path to the data, read at draw time. The
serializable alternative to passing \code{data} to \code{\link[=draw]{draw()}}.}

\item{origin}{Optional Named character: Where each value came from. Normally
computed from which arguments were supplied; pass it only when restoring a
config that already carries provenance.}

\item{writer}{Optional Named character: Which interface wrote the config.}
}
\value{
\link{A3Config} object.
}
\description{
The seam between convenient input and a complete, validated object. \strong{Every
argument is optional}, which is what lets the published schema require
nothing.
}
\examples{
setup_A3Config(n_per_row = 30L)@n_per_row
}
\author{
EDG
}
